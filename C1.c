// Write a Program to calculate and display the volume of a CUBE having its height (h=10cm) width (w=12cm) and depth (8cm).

#include <stdio.h>
int main()
{
    int x=10 , y=12, z=8, V;
    V = x*y*z;
    printf("The volume of the cube is: %i",V);
}
